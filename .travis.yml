dist: xenial
language: python

cache: pip

branches:
  only:
    - master

python:
  - "3.5"
  - "3.7"

env:
  - DJANGO_VERSION=2.0.*
  - DJANGO_VERSION=2.1.*
  - DJANGO_VERSION=2.2.*

stages:
  - Check
  - Docs
  - Test

jobs:
  include:
    - stage: Check
      python: 3.7
      script: scripts/check
    - stage: Docs
      python: 3.7
      script: scripts/docs

script:
  - scripts/test

after_script:
  - if [ -f .coverage ]; then
    python -m pip install codecov;
    codecov --required;
    fi
